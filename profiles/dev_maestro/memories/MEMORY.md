SquadGain Maestro: default platform is BOTH (`targetPlatform=null` in maestro_test_runner.dart line 28). Per-test ML/pose/camera tests opt out via `# platforms: android-only` header below `---`. Pinned: 4_2, 4_3. iOS sim build works with plain `flutter build ios --simulator --debug --target=lib/proto.dart` — Podfile's `ios/scripts/apple_silicon_simulator.rb` patches GoogleMLKit for arm64 sim, supersedes the legacy `build_simulator.sh`. Pod install ~80s + xcodebuild ~65s cold.
§
SquadGain Android: NDK 28.2.13676358 license + flutter_local_notifications desugaring blocked `flutter build apk --flavor dev`. Fix: ANDROID_HOME=~/Library/Android/sdk, run `sdkmanager --licenses`, and add `isCoreLibraryDesugaringEnabled = true` + `coreLibraryDesugaring("com.android.tools:desugar_jdk_libs:2.1.4")` to android/app/build.gradle.kts. Build now works in ~2 min producing 426 MB app-dev-debug.apk. ADB at /opt/homebrew/bin/adb. Use --no-build on test runner to skip rebuild.
§
SquadGain Maestro test pattern (mandatory): every story test boots via a populated HomeView Proto config (HomeView -> Home Demo / 5 workouts / PendingMemberApproval / DeeplinkInvite / WaitingForTeamApproval / Due Workout Today / etc. — these run setUpTeam and seed real members), then navigates with semanticLabels (userProfileButton, teamsButton, homeAddWorkoutButton, settingsButton) to the feature under test. Direct Proto-tile entry is only allowed when no Home path exists: workOutScreen execution, Challenge Screen execution, Team Progress Test, Circular Indicator Test. Screenshot names in YAML must be bare slugs ('story_1_6_03_team_loaded'), never prefixed with 'screenshots/' — the test runner organizes them into maestro/screenshots/android/ automatically.
§
SquadGain Maestro: tests need `- clearState` before every `- launchApp` (applied to all 24 story YAMLs) — without it, state leaks between tests in a `… all` run, every test after #1 lands on the prior test's screen instead of the Proto picker. Kill stale `maestro.cli.AppKt mcp` java processes with `pkill -f 'maestro.cli.AppKt mcp'` before any run; they hold ADB tcp-forwarders and block launchApp.
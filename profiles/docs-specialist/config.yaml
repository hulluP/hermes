model:
  provider: custom
  base_url: "http://localhost:6655/litellm/v1"
  default: "sonar-pro"
  context_length: 200000

# sonar-pro has web search built-in — no tool definitions needed
toolsets: []
agent:
  tool_use_enforcement: none

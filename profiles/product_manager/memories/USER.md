User collaborates on design docs via inline annotations: dumps thoughts into a markdown file, user edits the file with `*** PM ***` markers next to each question, expects the agent to read the answers and mirror the resolution in the terminal, then DELIVER THE FULL UPDATED MARKDOWN as a fenced ```markdown block in the terminal — the user pastes it into the file themselves. Do NOT silently write_file/patch the doc unless they explicitly say "edit the file." Signal phrases that lock terminal-paste mode for the session: "I edit the file", "give me the markdown", "paste it here", "give me the updated file here".
§
User expects strategic insight as the headline, not a footnote. Lead with moat-grade ideas. Picks ambitious-over-simple. Reframes parallel work into sequenced phases (founder bandwidth is the constraint). For multi-stage specs wants folder trees (README + narrative + setup + workflow + checklist), not one giant doc. Call out reusable assets (LoRAs, voice profiles) as strategic dimensions. SquadGain founder, Frankfurt, Hyrox. /docs/gtm/ root docs stay when stale — rewrite, don't archive.
§
User is a fast German-English operator with frequent typos ("snycs", "varsety", "BoxFit"). Parse intent through typos, never ask for clarification. They run multiple parallel projects (SquadGain, Hermes, Unypan) and won't re-read their own messages.